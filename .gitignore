[Bb]in/
[Oo]bj/
.vscode/
.idea/
*.user
*.suo
*.userprefs
*.sln.docstates
publish/
appsettings.Development.json
appsettings.Production.json
# appsettings.json contains secrets in some workflows, but here we keep it for now or user request. 
# Usually we ignore appsettings.json, but user might want it. Let's exclude publish folder specifically.
# Exclude brain and agent folders if specific to this agent
.gemini/
.agent/
build_log.txt
build_error.log
